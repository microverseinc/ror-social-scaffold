<%= form_for(@post) do |form| %>
  <div class="field">
    <div class="control">
      <%= form.label 'Create a new post', class: 'label' %>
    </div>
  </div>
  <div class="field">
    <div class="control">
      <%= form.text_field :content, id: :post_content, class: 'textarea', placeholder: 'Add New Post', wrapper: false %>
    </div>
  </div>
  <div class="field">
    <div class="control">
      <%= display_errors(@post) %>
    </div>
  </div>
  <div class="field">
    <div class="control">
      <%= form.submit 'New Post', class: 'button is-primary' %>
    </div>
  </div>
<% end %>
