<%= form_with model: @friendship, url: friendship_path(user), local: true do |form| %>
<%= form.hidden_field :requester_id, value: user.id %>
 
<%= form.submit 'Accept', name: 'accept', method: 'put'%>
<br>
<br>
<%= form.submit 'Reject', name: 'accept', method: 'delete'%>
<% end %>