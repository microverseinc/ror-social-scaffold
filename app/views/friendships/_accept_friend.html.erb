<%= form_with model: @friendship, url: friendship_path(user), method: method, local: true do |form| %>
<%= form.hidden_field :requester_id, value: user.id %>
 
<%= form.submit action_text %>

<% end %>