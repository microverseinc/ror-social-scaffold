<li>
  Name: <%= friendship.name %>
  <span class="profile-link">
    <%= link_to 'See Profile',  user_path(friendship), class: 'profile-link' %>
  </span> 
  <% unless current_user.friend?(friendship) %> 
  | <button><%= link_to 'Accept',  friendship_path(id: friendship.id), method: 'patch' %> </button> 
  | <button> <%= link_to 'Decline',  friendship_path(id: friendship.id), method: 'delete' %>
</button> <% end %>
</li>