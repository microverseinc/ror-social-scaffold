<%= form_with(model: current_user.friendships.build, local: true) do |f| %>
  <div><%= hidden_field_tag :friend_id, @user.id %></div>
  <%= f.submit "Send Friend Request", class: "btn btn-primary" %>
<% end %>