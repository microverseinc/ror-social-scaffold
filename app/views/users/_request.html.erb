<% if !current_user?(user) %>
  <%= form_with model: new_request(user), local: true do |form|%>
    <%= hidden_field_tag :friend_id, user.id %>
    <%= form.submit "Invite to friendship" %>
  <% end %>
<% end %>
