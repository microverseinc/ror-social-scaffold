<% if !current_user?(@user) %>
  <%= form_with model: @request, local: true do |form|%>
    <%= hidden_field_tag :friend_id, @user.id %>
    <%= form.submit "Invite to frinedship", class:"btn btn-success" %>
  <% end %>
<% end %>
