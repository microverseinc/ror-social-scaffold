<%= form_with model: request_recieved(user), local: true, html:{method: :patch} do |form|%>
  <%= hidden_field_tag :friend_id, user.id %>
  <%= hidden_field_tag :index, index %>
  <%= form.submit "Accept invitation" %>
<% end %>
