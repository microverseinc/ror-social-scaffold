<%= form_tag(friendship_path(friendreq), method: :patch) do |f| %>
  <%= submit_tag  'Accept' %>
<% end %>
<%= form_tag(friendship_path(friendreq), method: :delete) do |f| %>
  <%= submit_tag  'Decline', data: { confirm: 'Are you sure you want to decline?' } %>
<% end %>