<section class="user-section">
  <h2><%= "Name: #{@user.name}" %>     </h2>
   <%if(current_user!=@user)%>
   <%= button_to "Invite to Friendship",friendships_path(user_id:@user)%>
   <%end%>
  <article class="timeline">
    <h3>Recent posts:</h3>
    
    <ul class="posts">
    
      <%= render @posts %>
    </ul>
  </article>
</section>
