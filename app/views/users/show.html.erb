<section class="user-section">
  <h2><%= "Name: #{@user.name}" %></h2>
  <span class="profile-link">
    <% if sent_requests(@user.id) %>
      pending
    <% else %>
      <%= link_to 'invite to friendship', user_invite_path(@user), class: 'profile-link', method: :post%>
    <% end %>
  </span>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
