<section class="user-section">
  <h2><%= "Name: #{@user.name}" %></h2>
  <%= different_user(current_user.id, @user.id) %>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
