<section class="user-section">
  <div>
    <h2 class="d-inline"><%= "Name: #{@user.name}" %></h2>
    <%= friendship_action(@user) %>
  </div>

  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
