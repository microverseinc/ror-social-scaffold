<section class="user-section">
  <h2><%= "Name: #{@user.name}" %></h2>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <%= add_friend(@user) %>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
